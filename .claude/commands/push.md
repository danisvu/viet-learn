1. `git add .`
2. Phân tích diff, tạo commit message theo Conventional Commits
3. `git pull --rebase origin` nhánh hiện tại
4. `git push origin` nhánh hiện tại
5. Hiển thị kết quả

Không bao giờ dùng `--force`.
Xoá mọi attribution "Generated with Claude Code".
